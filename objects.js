// arrays

const arr = [1, 2, 3];

arr.length

console.log(arr[0]);

arr[0] = 2;

arr.push(1) --> //add item to end of array
arr.pop() --> // removes something from end of array
arr.unshift('somehting') --> // puts item at the beginning of array
arr.shift() --> // removes from the beginning of array
arr.reverse() --> // will flip the array around

// +=
// a = a + 1
// -=
// a = a - 1
// /=
// a = a / 1
// *=
// a = a * 1
// %=
// a = a % 1

// {} object

const turtle = {
	name: 'marvin',
	age: 102,
	hungry: true
};

// ways to change and add object properties
turtle.name = 'merdle the turtle';

turtle['hungry'] = false

const bird = {};

bird.name = 'dee';

const tree = {
	type: 'Oak',
	age: 124,
	color: 'brown',
	leaves: true,
};

const fish = {};

fish.name = 'Franklin';
fish.age = 2;
fish.size = 'small';
fish.type = 'Goldfish';
fish.color = ['red', 'blue', 'gold'];






