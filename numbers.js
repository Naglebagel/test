//old
var a = 1;

//new 
const a = 1;
//const are immutable, meaning they can't change

//let 
let b = 2;
//let variable you can change, is mnutable (a use case of this may be in loops)

const multiple = 9*9;
const divide  = 9/9;
const addition = 4+4;
const subtraction = 4-4;
const modulus = 10%4;

//variable names should be
//camelCase
const randNumber = Math.random();


Math.round(Math.random()*10);

//typeof 
//checks the data type
typeof(10); 
typeof 8;

//Whole number ex. 8 is an integer 
//Decimal ex. 8.19181 is a float 